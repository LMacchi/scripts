node default {
  # include profile::davita_base
  include new
  ini_setting { 'test':
    ensure  => absent,
    path    => '/etc/puppetlabs/puppet/puppet.conf',
    section => 'agent',
    setting => 'blah',
  }
}
